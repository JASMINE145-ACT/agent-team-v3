# Coze 企划 → Agent Team version3 映射草案

> 目标：把《Coze 助力 PT. VANTSING 企业微信 + ACCURATE 系统全业务流程实施方案》中的业务流程，系统性映射到 `Agent Team version3` 中的各类 Agent、工具与数据流，作为后续重构与扩展的蓝本。  
> **项目要努力达成的方向与优先级** 见：`项目方向与目标_基于Coze企划.md`。

---

## 1. 企划整体结构（摘录）

- **文档名称**：Coze 助力 PT. VANTSING 企业微信 + ACCURATE 系统全业务流程实施方案
- **主要章节**（来自原 Word）：
  - 一、方案总则（核心目标、适用范围、核心逻辑、前提条件）
  - 二、前期准备（企业微信配置、Coze 平台准备、ACCURATE 系统准备、业务流程梳理与标准化）
  - 三、核心业务流程实施方案：环节1 报价 → 环节2 库存查询 → 环节3 成单 → 环节4 采购 → 环节5 送货跟踪 → 环节6 收款
  - 四、Coze 智能体与工作流核心配置（Prompt、6 大工作流编排、权限、ACCURATE 对接参数）
  - 五、实施步骤与时间规划
  - 六、风险防控与优化措施
  - 七、预期效果；八、后期维护与支持

---

## 2. 角色 / 岗位 → Agent 映射

- **2.1 角色列表**
  - TODO：从企划中提取「销售」「客服」「仓库」「财务」「运营」等角色及其职责

- **2.2 Agent 设计（初稿）**
  - 示例结构（待按企划补充或修改）：
    - 销售助手 Agent：负责在企微内帮销售做价格查询、库存查询、报备与跟进记录。
    - 订单处理 Agent：负责从企微/表格中识别订单信息，落地到 ACCURATE / 本地数据库。
    - 仓库协同 Agent：负责无货登记、缺货记录与邮件通知，复用 version3 现有无货/缺货链路。
    - 财务对账 Agent：围绕 ACCURATE/其他系统，辅助对账、开票与回款跟进。
    - 运营分析 Agent：基于历史询价、成交记录输出策略建议（如「哪些产品多次无货」「哪些客户复购频率高」）。

> 使用方式：当你从企划中看到新的角色或岗位时，在本节补充，并标注「是否需要独立 Agent」还是由现有 Agent 扩展能力。

---

## 3. 关键业务流程 → Agent 协作链路

- **3.1 客户触达 / 线索获取**
  - 企划中的描述：TODO（粘贴原文或用自己话概括）
  - 建议的 Agent 协作：
    - TODO：列出涉及的 Agent（如 销售助手 Agent、运营分析 Agent 等）及其调用顺序。

- **3.2 询价与报价**
  - 企划中的描述：TODO
  - 与 `Agent Team version3` 现状的关系：
    - 已有：报价填表 Work Mode、万鼎选型、inventory 查询等。
    - 待对齐：TODO（例如「是否需要把企划里的某些场景映射到现有工具 / 新增工具」）。

- **3.3 下单与执行（订单、发货、库存变动）**
  - 企划中的描述：TODO
  - 建议的 Agent 协作：TODO

- **3.4 售后、回访与复购**
  - 企划中的描述：TODO
  - 建议的 Agent 协作：TODO

> 使用方式：每看到企划中的一个完整业务流程，就在本节增加一个小节（3.x），写清「起点 → 中间环节 → 终点」及每一步对应的 Agent/工具。

---

## 4. 与 Agent Team version3 现有能力的对齐

- **4.1 已有能力直接覆盖的场景**
  - 库存查询 Agent（inventory_agent）：覆盖企划中「查库存」相关场景。
  - 询价三工具 + Work Mode：覆盖企划中「报价、填表、历史匹配」等场景。
  - 无货/缺货登记 + 邮件：覆盖企划中「缺货提醒、邮件通知」等场景。

- **4.2 需要新增 / 重构的能力**
  - TODO：按企划需求列出当前 version3 中没有或需要明显改造的点，例如：
    - 企微侧的交互流程是否需要更贴合企划中的话术与入口。
    - 是否需要新增某类「订单流转 Agent」「财务对账 Agent」等。

- **4.3 项目结构与文档的挂钩**
  - 对照 `claude.md` 中对 `Agent Team version3` 的描述，补充：
    - 本文档中的每个 Agent/流程小节，对应到哪些代码目录 / 模块（例如 `backend/tools/...`、`backend/agent/...`）。

---

## 5. 后续演进计划（Roadmap 草案）

- **阶段 1：对齐企划，做「映射表」**
  - 补全本文件第 1～4 节，保证企划中的每个角色、流程都能在本文件中找到对应的 Agent 或 TODO。

- **阶段 2：按业务价值排序落地**
  - 从企划中选出 2～3 条最重要的业务闭环（例如「企微询价 → 报价 → 无货登记 → 邮件」），在 version3 里做端到端串联与验证。

- **阶段 3：扩展与优化**
  - 把剩余流程逐步纳入 `Agent Team version3`，同时优化 prompts、工具设计与监控指标。

> 后续当我们一起把企划内容一点点补进来时，这个文档会从「草案」升级为 version3 的正式业务蓝图。

