feat: add WeCom application message API relay for OOS/shortage alerts

- New POST /agent/notify endpoint (routes_notify.py) with support for
  to_users / to_parties / to_tags and text / markdown msg_type
- WeComApplicationClient (notification_service.py) with token cache and
  pipe-separated target building
- alert_dispatch.py: wecom_only now prefers application message API
  (WECOM_APP_NOTIFY_USERS) over Webhook, with Webhook as fallback
- oos/config.py: add WECOM_APP_NOTIFY_USERS env var
- oos/.env.example and wecom_bot/.env.example: document new WECOM_APP_*
  variables and WECOM_GROUP_ALIAS_MAPPING
- oos-email-wecom-alerts.md: document the new relay mechanism and group
  broadcast constraint
