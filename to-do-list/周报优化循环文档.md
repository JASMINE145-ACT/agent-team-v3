# 周报优化循环文档（Ralph Loop）

## Loop 元信息

- 当前迭代：7
- 目标：持续完善周报内容逻辑机制 + 前端优化方案
- 更新规则：每次 loop 追加“本轮新增”与“下一轮行动”

---

## 一、现状速览（基于 Trellis 索引与代码）

### 1) 周报后端现状

- 主流程为两阶段：
  - Phase 1 同步：抓取 Accurate 发票 -> pandas 分析 -> 生成 `report_md` -> 入库。
  - Phase 2 异步：后台线程调用 LLM 生成 `analysis_md`。
- 关键代码：
  - `backend/reports/runner.py`
  - `backend/reports/llm_analyzer.py`
  - `backend/server/api/routes_reports.py`
- 当前已有能力：
  - 支持历史记录、重跑分析、重格式化；
  - 支持基础环比（`prev_week`）；
  - 通过 `analysis_status` + 前端轮询感知异步分析进度。

### 2) 前端现状

- 周报页在 `control-ui/src/ui/views/reports-tab.ts`，已具备：
  - 数据/分析双 tab；
  - 图表 + 表格展示；
  - 分析失败重跑；
  - 分析轮询（3 秒，最多 80 次）。
- 控制器在 `control-ui/src/ui/controllers/reports.ts`，核心机制是轮询 + 软刷新（`soft=true`）。

---

## 二、周报“内容逻辑机制”可行优化措施

## A. 内容层（报告可读性、决策价值）

- 增加固定结构：`一页总览 -> 异常点 -> 归因 -> 行动建议 -> 下周预测区间`，减少“有数据但难决策”。
- 增加“异常规则卡”：
  - 单日销售额突降/突增阈值；
  - 订单数与销售额背离（客单价异常）；
  - Top 客户集中度过高风险（CR3/CR5）。
- 将 `analysis_md` 分段标准化输出（固定标题），便于前端渲染锚点导航和后续自动摘要。

## B. 逻辑层（数据正确性、可解释性）

- 在 Phase 1 入库前新增数据质量检查（DQ Gate）：
  - 周区间完整性；
  - `total_order_count` 与 `daily_stats` 聚合一致性；
  - 金额非负与异常值检测（可配置阈值）。
- 增加“环比基线兜底”：
  - 若上周缺失，回退最近可用周；
  - 同时在报告中明确“基线来源周”，避免误解。
- 把 `llm_analyzer` 的预测部分改为“公式化输入”：
  - 先在后端算出趋势指标（均值、方差、周内波动、Top 客户稳定性）；
  - LLM 仅解释，不让其自行隐式推导关键数字。

## C. 机制层（稳定性、可运维性）

- 将 Phase 2 从进程内线程升级为可观测后台任务（队列/worker）：
  - 避免进程重启导致分析中断；
  - 提供重试次数、死信、耗时统计。
- 增加 SLA 指标：
  - 抓取耗时、分析耗时、失败率、超时率；
  - 暴露到管理面板。
- 调度增强：
  - 增加“错过调度自动补跑”（例如服务重启后补上上一周期）。

---

## 三、前端可行优化措施

## A. 交互与信息架构

- 历史记录列表支持筛选/搜索（按日期、状态、触发方式），避免记录变多后定位困难。
- 详情页新增“摘要卡片”：
  - 环比变化、异常告警数量、Top 客户集中度；
  - 默认先看结论，再看明细图表。
- 分析内容增加目录锚点（趋势/环比/建议/预测），提高可读性。

## B. 性能与实时性

- 将轮询升级为“优先 SSE/WebSocket，失败回退轮询”：
  - 减少无效请求；
  - 提升状态变更感知实时性。
- 图表数据量增大时做降采样与懒渲染（先渲染总览，再按需渲染明细）。
- 历史记录超过阈值后使用分页或虚拟列表，避免长列表渲染压力。

## C. 可用性与运维友好

- 超时文案附带“可执行下一步”（检查 token、检查后端日志、点击重试）。
- 在 UI 展示分析任务时长与最后错误摘要，减少排障成本。
- 任务配置区增加 cron 预览（下一次执行时间）与时区提示，降低误配置概率。

---

## 四、外部实践（用于对照本项目）

- 自动化周报最佳实践共识：
  - 控制核心指标数量（避免指标泛滥）；
  - 报告结构固定化（Scorecard + Highlights + Risks + Actions）；
  - 建立数据质量门禁，避免“自动化放大脏数据”。
- 前端仪表盘优化共识：
  - 高频状态不宜长期轮询，建议事件推送；
  - 大列表用虚拟化，大图表做采样/分层渲染；
  - 状态更新与视图渲染解耦，减少整页重渲染。
- 参考链接：
  - https://www.astrato.io/blog/automated-scheduled-reporting-playbook
  - https://www.sylus.ai/feeds/blog/how-to-automate-reports
  - https://monday.com/blog/crm-and-sales/best-practices-ai-sales-report-automation/
  - https://www.frontenddigest.com/system-design/design-dashboard-analytics
  - https://www.wellally.tech/blog/build-real-time-health-dashboard-react-websockets-d3

---

## 五、本轮新增结论（Iteration 1）

- 已完成 Trellis 索引定位，锁定周报主链路后端与前端文件。
- 已提出可落地优化框架：内容层、逻辑层、机制层 + 前端三层优化。
- 已建立循环文档，后续每轮在此文件追加更新。

## 六、下一轮行动（Iteration 2 计划）

- 细化为“优先级路线图”（P0/P1/P2）+ 预估改造成本（低/中/高）。
- 补充数据库层改造草案（新增字段、索引、任务事件表）。
- 输出前端 API 与状态机改造草图（轮询 -> 推送优先）。

---

## 七、本轮新增结论（Iteration 2）

- 已完成门禁修复并落地两项关键稳态改进：
  - `backend/reports/analyzer.py` 增加周区间硬过滤（防上游日期异常污染周报）；
  - `backend/server/api/routes_reports.py` 的 `reanalyze` 增加并发互斥与状态安全更新（避免重复触发和脏状态锁死）。
- 已补充回归测试并通过：
  - `tests/test_reports_analyzer.py`
  - `tests/test_reports_routes.py`
- 已验证门禁链路：
  - code-review agent：PASS（无阻塞问题）
  - test-agent：PASS（`6 passed, 0 failed`）

## 八、优先级路线图（P0 / P1 / P2）

## P0（本周应做，低风险高收益）

- 周报分析链路加“DQ Gate 明细输出”（校验项 + 告警计数写入 `summary_json`）。
- `reanalyze` 返回结构增加可观测字段（例如 `enqueued_at`、`record_id`、`current_status`）。
- 前端失败态增加“下一步排障指引”与一键复制错误摘要。

## P1（1-2 周，结构性优化）

- 调度可靠性增强（APScheduler `misfire_grace_time` + `coalesce` + missed-run 补跑策略）。
- 分析状态从纯轮询升级为“推送优先（SSE）+ 轮询兜底”。
- 历史记录支持筛选与分页，降低长列表渲染与定位成本。

## P2（2-4 周，系统升级）

- 将 LLM 分析从进程内线程迁移到可观测 worker 队列（重试/死信/耗时指标）。
- 引入“报告质量评分”（数据完整度、异常覆盖率、解释一致性）用于持续迭代。
- 增加“同比/多周趋势模式”与分层权限分发（管理层摘要 vs 运营细节）。

## 九、数据库改造草案（v0）

- `report_records` 建议新增：
  - `dq_checks_json JSONB`：DQ 逐项结果；
  - `analysis_started_at TIMESTAMPTZ`、`analysis_finished_at TIMESTAMPTZ`；
  - `analysis_retry_count INT DEFAULT 0`。
- 新增 `report_analysis_events`（事件流水）：
  - 字段：`id`, `record_id`, `event_type`, `event_payload`, `created_at`；
  - 用途：追踪状态迁移与错误归因。
- 索引建议：
  - `idx_report_records_task_week` on `(task_key, week_start DESC)`；
  - `idx_report_records_analysis_status` on `(analysis_status, started_at DESC)`；
  - `idx_report_analysis_events_record_created` on `(record_id, created_at DESC)`。

## 十、前端 API / 状态机草图（轮询 -> 推送优先）

- API 草图：
  - `GET /api/reports/records/{id}/events/stream`（SSE，推送 analysis 状态变化）；
  - `GET /api/reports/records/{id}`（保留，作为断线重连和兜底轮询）。
- 状态机草图：
  - `idle -> pending -> running -> done|failed`；
  - `failed -> retry_requested -> pending`；
  - 若 SSE 断开：进入 `fallback_polling`，恢复连接后返回 `live_streaming`。
- 前端策略：
  - 优先订阅 SSE；
  - SSE 不可用时按指数退避轮询（3s -> 5s -> 8s，上限 30s）；
  - 仅对当前选中记录保持高频更新，其他记录低频刷新。

## 十一、外部资料增量（Iteration 2）

- 调度可靠性（APScheduler）：
  - https://apscheduler.readthedocs.io/en/master/userguide.html
- 实时状态更新架构（Polling vs SSE）：
  - https://koder.ai/blog/websockets-vs-sse-live-dashboards
  - https://potapov.me/en/make/websocket-sse-longpolling-realtime

## 十二、下一轮行动（Iteration 3 计划）

- 把 P0 拆成具体 issue 清单（目标、验收标准、风险、回滚方案）。
- 设计 `report_analysis_events` 的最小可用版本（DDL + 写入点 + 查询接口）。
- 输出前端 SSE 接入最小改造 diff 清单（controller/view/state 三层）。

---

## 十三、本轮新增结论（Iteration 3）

- 已将 P0 方案进一步拆到可执行级别（目标/验收/回滚）。
- 已补充 SSE 接入的工程细节（`Last-Event-ID`、断线续传、兜底轮询策略）。
- 已新增前端可访问性优化建议（对比度、键盘可达、图表文字等价信息）。
- 门禁验证通过：
  - code-review agent：PASS（无阻塞问题）
  - test-agent：PASS（`6 passed, 0 failed`）

## 十四、P0 Issue 拆解（可直接建任务）

## Issue P0-1：DQ Gate 结果落库与展示

- 目标：
  - 在报告生成后输出 DQ 检查结果到 `summary_json` 或 `dq_checks_json`。
- 验收标准：
  - API 返回中包含 `dq_passed`、`warnings_count`、`failed_rules`；
  - 前端记录卡片可见 DQ 状态徽标；
  - 至少覆盖 3 条规则（日期完整性、聚合一致性、金额合法性）。
- 风险：
  - 规则过严导致误报，影响业务信任。
- 回滚方案：
  - DQ 改为仅告警不阻断；通过 feature flag 关闭展示。

## Issue P0-2：reanalyze 可观测性增强

- 目标：
  - `POST /reanalyze` 返回统一可观测字段（`record_id`, `status`, `requested_at`）。
- 验收标准：
  - API 文档明确字段语义；
  - 前端成功提示包含状态与时间戳；
  - 错误返回保留 409/422 语义区分。
- 风险：
  - 前端旧逻辑依赖当前返回结构。
- 回滚方案：
  - 增加向后兼容字段并保留旧字段一版周期。

## Issue P0-3：前端失败态排障助手

- 目标：
  - 分析失败/超时时提供可执行排障引导与一键复制错误上下文。
- 验收标准：
  - UI 包含“检查 token/检查后端日志/重试”三步；
  - 可复制字段至少包括 `record_id`, `analysis_status`, `last_error`；
  - 文案支持 i18n。
- 风险：
  - 提示信息过长影响可读性。
- 回滚方案：
  - 保留折叠版帮助区，仅默认展示首条建议。

## 十五、SSE 最小接入方案（Iteration 3 草案）

- 后端接口草案：
  - `GET /api/reports/records/{id}/events/stream`
  - 事件：`analysis.pending`, `analysis.running`, `analysis.done`, `analysis.failed`
  - 每条事件带 `id`（递增）+ `data`（状态快照）
- 重连策略：
  - 客户端基于 `EventSource` 自动重连；
  - 服务端读取 `Last-Event-ID` 回放缺失事件；
  - 如果事件窗口已过期，返回“需重载快照”信号，前端调用详情接口补齐。
- 心跳与兜底：
  - 心跳注释帧每 15-30 秒发送一次；
  - SSE 断开后降级指数退避轮询（3s -> 5s -> 8s -> 13s，上限 30s）。

## 十六、前端可访问性优化清单（新增）

- 图表不可仅靠颜色传达信息：
  - 增加数据标签/形状差异/文本摘要。
- 键盘可达：
  - 记录列表、tab、重试按钮必须可 `Tab` 导航与可见焦点。
- 对比度：
  - 关键文本与背景至少满足 4.5:1（最低不低于 3:1）。
- 语义与读屏：
  - 图表区域提供 `aria-label` 与“本周关键结论”文本替代块；
  - 分析状态变化可通过 `aria-live` 区域播报。

## 十七、外部资料增量（Iteration 3）

- SSE 规范与重连：
  - https://html.spec.whatwg.org/C/server-sent-events.html
  - https://codelit.io/blog/sse-server-sent-events-guide
- 仪表盘可访问性：
  - https://deque.com/blog/how-to-make-interactive-charts-accessible
  - https://www.gooddata.com/docs/cloud-native/latest/create-dashboards/accessibility/
  - https://www.highcharts.com/blog/best-practices/best-chart-accessibility-practices/

## 十八、下一轮行动（Iteration 4 计划）

- 输出 `report_analysis_events` MVP 的 DDL + 写入时机伪代码。
- 产出 SSE 接口契约草案（事件类型、payload schema、错误语义）。
- 给 `reports-tab.ts` 制定无障碍改造最小改动清单（可直接分配到前端任务）。

---

## 十九、本轮新增结论（Iteration 4）

- 已完成 `report_analysis_events` MVP 设计（DDL + 写入点 + 查询接口草图）。
- 已形成 SSE 契约草案（事件类型、payload 结构、重连/补偿语义）。
- 已给出 `reports-tab` 无障碍最小改造清单（可直接拆前端任务）。
- 门禁验证通过：
  - code-review agent：PASS（无阻塞问题）
  - test-agent：PASS（`6 passed, 0 failed`）

## 二十、`report_analysis_events` MVP 设计（落地版）

## 1) DDL 草案

```sql
CREATE TABLE IF NOT EXISTS report_analysis_events (
  id BIGSERIAL PRIMARY KEY,
  record_id BIGINT NOT NULL REFERENCES report_records(id) ON DELETE CASCADE,
  event_id TEXT NOT NULL,                    -- SSE id / 幂等键（建议 record_id-seq）
  event_type TEXT NOT NULL,                  -- analysis.pending|running|done|failed|heartbeat
  event_payload JSONB NOT NULL DEFAULT '{}'::jsonb,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  request_id TEXT NULL
);

CREATE UNIQUE INDEX IF NOT EXISTS ux_report_analysis_events_event_id
  ON report_analysis_events(event_id);

CREATE INDEX IF NOT EXISTS idx_report_analysis_events_record_created
  ON report_analysis_events(record_id, created_at DESC);
```

## 2) 写入时机（伪代码）

```python
def emit_event(record_id, event_type, payload):
    insert report_analysis_events(...)

def run_llm_analysis(...):
    emit_event(record_id, "analysis.pending", {...})
    set_status("running")
    emit_event(record_id, "analysis.running", {...})
    try:
        # call llm
        set_status("done")
        emit_event(record_id, "analysis.done", {"summary": "...", "duration_ms": ...})
    except Exception as e:
        set_status("failed")
        emit_event(record_id, "analysis.failed", {"error": str(e)})
```

## 3) 查询接口草图

- `GET /api/reports/records/{id}/events?after_id=123&limit=200`
  - 返回增量事件，供断线补偿与调试用。

## 二十一、SSE 接口契约草案（v0.1）

## 1) Stream API

- `GET /api/reports/records/{id}/events/stream`
- 必要响应头：
  - `Content-Type: text/event-stream`
  - `Cache-Control: no-cache`
  - `Connection: keep-alive`
  - `X-Accel-Buffering: no`（防代理缓冲）

## 2) 事件定义

- `event: analysis.pending`
- `event: analysis.running`
- `event: analysis.done`
- `event: analysis.failed`
- `event: stream.snapshot_required`（`Last-Event-ID` 过旧，提示客户端回拉详情）

统一 payload 示例：

```json
{
  "record_id": 123,
  "analysis_status": "running",
  "ts": "2026-04-30T07:20:00Z",
  "message": "analysis started",
  "meta": { "attempt": 1 }
}
```

## 3) 重连语义

- 客户端断开重连时自动携带 `Last-Event-ID`。
- 服务端若能回放则补发缺失事件；不能回放则发 `stream.snapshot_required`。
- 客户端收到 `stream.snapshot_required` 后立即调用 `GET /api/reports/records/{id}` 对齐状态。

## 二十二、`reports-tab.ts` 无障碍最小改造清单（可分配任务）

## A. 交互可达

- 历史记录按钮增加 `aria-label`（包含日期、状态、金额）。
- 详情 tab 按钮增加 `aria-selected` 与键盘方向键切换支持。
- “重新分析”按钮在失败态与超时态都可聚焦且文案一致。

## B. 图表可读

- 每个图表下方增加“文本摘要块”（最高日/最低日/Top 客户贡献）。
- 图表颜色增加备用视觉编码（图例标记/文本标签），避免仅靠颜色。

## C. 状态播报

- 分析状态容器增加 `aria-live="polite"`，在 `pending/running/done/failed` 切换时播报。
- 轮询超时文案增加可读短句，避免仅图标提示。

## 二十三、外部资料增量（Iteration 4）

- 事件日志表设计与索引：
  - https://viprasol.com/blog/postgres-event-sourcing/
  - https://appmaster.io/blog/postgresql-partitioning-event-audit-tables
- SSE 生产部署注意项（缓冲/心跳/重连）：
  - https://serverfault.com/questions/801628/for-server-sent-events-sse-what-nginx-proxy-configuration-is-appropriate
  - https://stackoverflow.com/questions/61029079/how-to-turn-off-buffering-on-nginx-server-for-server-sent-event
  - https://webcoderspeed.com/blog/scaling/server-sent-events-production

## 二十四、下一轮行动（Iteration 5 计划）

- 把 DDL 草案映射到项目现有 `models.py` 迁移路径（最小改动版）。
- 输出 SSE 后端路由与前端 controller 改造 diff 清单（按文件）。
- 给前端无障碍改造增加验收测试建议（键盘导航 + aria-live + 对比度校验）。

---

## 二十五、本轮新增结论（Iteration 5）

- 已将“设计项”进一步下沉到文件级改造清单（后端/前端按文件拆分）。
- 已补充 FastAPI SSE 路由实现注意点（断连检测、心跳、代理缓冲）。
- 已补充前端 EventSource 回退策略与测试建议。
- 本轮发现并修复一个阻塞缺陷：
  - `reanalyze_record` 对坏 `report_json` 字符串可能抛 500，现已改为受控 422；
  - 新增回归测试覆盖 malformed JSON 场景。
- 门禁验证通过：
  - code-review agent：PASS（无阻塞问题）
  - test-agent：PASS（`7 passed, 0 failed`）

## 二十六、文件级改造清单（可直接开工）

## 后端（Phase A）

- `backend/reports/models.py`
  - 新增 `report_analysis_events` 建表与索引（`ensure_tables()` 内最小迁移）。
  - 预留 `analysis_started_at/analysis_finished_at/dq_checks_json` 列（`ALTER TABLE IF NOT EXISTS` 风格）。
- `backend/reports/llm_analyzer.py`
  - 引入 `emit_analysis_event()`；
  - 在 pending/running/done/failed 节点写事件。
- `backend/server/api/routes_reports.py`
  - 新增 `GET /api/reports/records/{record_id}/events/stream`（SSE）；
  - 新增 `GET /api/reports/records/{record_id}/events`（增量拉取补偿）。
- `backend/reports/service.py`
  - 若采用后台 worker，预留事件消费者启动钩子（本阶段可先不启用）。

## 前端（Phase B）

- `control-ui/src/ui/controllers/reports.ts`
  - 新增 `startAnalysisEventStream(state, id)` 与 `stopAnalysisEventStream()`；
  - 连接失败自动降级到现有轮询，并带退避节奏。
- `control-ui/src/ui/views/reports-tab.ts`
  - 新增 `aria-live` 状态播报区域；
  - 历史记录按钮加 `aria-label`；
  - Tab 加 `aria-selected` + 键盘左右切换。
- `control-ui/src/ui/types.ts`
  - 新增 `ReportAnalysisEvent` 类型定义（`id/type/payload/ts`）。

## 二十七、SSE 路由实现要点（FastAPI 版）

- 建议使用 `StreamingResponse` 或 `EventSourceResponse` 输出 `text/event-stream`。
- 循环内必须检测断连：
  - `await request.is_disconnected()` 为 true 时及时释放资源。
- 心跳建议：
  - 每 15~30 秒发送注释帧 `: keepalive` 防止中间层超时。
- 代理配置注意：
  - 返回 `X-Accel-Buffering: no`；
  - 配合 Nginx 关闭 `proxy_buffering`，避免事件堆积后批量下发。

## 二十八、前端 EventSource 回退策略（可执行）

- 优先策略：
  - 进入分析页后立即启动 EventSource。
- 失败回退：
  - 连续 N 次错误（建议 3 次）后进入轮询模式；
  - 轮询退避：`3s -> 5s -> 8s -> 13s -> 21s -> 30s`。
- 恢复策略：
  - 用户手动刷新详情或页面重新聚焦时，重试恢复 SSE。
- 一致性策略：
  - 每次收到 SSE `done/failed` 事件后，强制调用一次详情接口做最终对齐。

## 二十九、前端验收测试建议（Iteration 5 新增）

- 功能测试：
  - SSE 正常推送时，analysis 状态可从 pending/running 切换到 done。
  - 断开 SSE 后自动回退轮询，最终仍能到达 done/failed。
- 可访问性测试：
  - 键盘可遍历历史记录、tab、重试按钮，焦点可见；
  - `aria-live` 在状态变化时有播报；
  - 关键文本对比度达到 4.5:1（最低 3:1）。
- 回归测试：
  - malformed `report_json` 返回 422（已补测试）；
  - reanalyze 并发冲突仍返回 409。

## 三十、外部资料增量（Iteration 5）

- FastAPI SSE / Streaming 实践：
  - https://fastapi.tiangolo.com/es/tutorial/server-sent-events/
  - https://medium.com/%40ThinkingLoop/10-fastapi-patterns-for-streamed-responses-1bab92860a0e
- 前端 EventSource 重连策略参考：
  - https://github.com/jstolwijk/reconnecting-event-source
  - https://github.com/joshmossas/event-source-plus

## 三十一、下一轮行动（Iteration 6 计划）

- 产出 `routes_reports.py` 的 SSE 端点伪实现草稿（含事件格式）。
- 产出 `reports.ts` 的 SSE 接入伪代码（含回退轮询状态机）。
- 将 DQ Gate 规则从“文档定义”推进到“代码接口定义”（字段与返回契约）。

---

## 三十二、本轮新增结论（Iteration 6）

- 已把上一轮计划推进到“接口与字段契约”级别：
  - 输出后端 SSE 端点伪实现结构；
  - 输出前端 `reports.ts` 接入伪代码骨架；
  - 输出 DQ Gate 字段契约（便于后端/前端并行开发）。
- 新增了 DQ Scorecard 维度化建议（准确性、完整性、时效性、一致性、唯一性）。
- 新增了 Lit 侧性能策略（`repeat`/`guard`/virtualizer）用于报告历史列表与大表格优化。
- 门禁验证通过：
  - code-review agent：PASS（无阻塞问题）
  - test-agent：PASS（`7 passed, 0 failed`）

## 三十三、`routes_reports.py` SSE 端点伪实现草稿

```python
@router.get("/api/reports/records/{record_id}/events/stream")
async def stream_report_events(record_id: int, request: Request, x_reports_token: str | None = Header(default=None)):
    _require_reports_token(x_reports_token)
    last_event_id = request.headers.get("last-event-id")

    async def event_gen():
        # 1) 若 last_event_id 可回放，先补发增量事件
        for evt in load_events_since(record_id, last_event_id):
            yield sse(evt.event_id, evt.event_type, evt.event_payload)

        # 2) 持续流：监听新事件（或轮询 events 表）
        while True:
            if await request.is_disconnected():
                break
            evt = await next_event_or_timeout(record_id, timeout_seconds=15)
            if evt is None:
                yield ": keepalive\n\n"
                continue
            yield sse(evt.event_id, evt.event_type, evt.event_payload)

    return StreamingResponse(
        event_gen(),
        media_type="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "Connection": "keep-alive",
            "X-Accel-Buffering": "no",
        },
    )
```

## 三十四、`reports.ts` 接入伪代码骨架（SSE + fallback）

```ts
function startAnalysisEventStream(state: ReportsState, id: number) {
  stopAnalysisEventStream();
  const url = apiUrl(state.basePath, `/api/reports/records/${id}/events/stream`);
  const es = new EventSource(url, { withCredentials: false });
  let failCount = 0;

  es.addEventListener("analysis.running", () => failCount = 0);
  es.addEventListener("analysis.done", async () => {
    await loadReportDetail(state, id, { soft: true });
    stopAnalysisEventStream();
  });
  es.addEventListener("analysis.failed", async () => {
    await loadReportDetail(state, id, { soft: true });
    stopAnalysisEventStream();
  });
  es.onerror = () => {
    failCount += 1;
    if (failCount >= 3) {
      stopAnalysisEventStream();
      startAnalysisPoller(state, id); // 退回现有轮询
    }
  };
}
```

## 三十五、DQ Gate 字段与返回契约（v0.1）

- `summary_json` 增补：
  - `dq_score`: number (0-100)
  - `dq_passed`: boolean
  - `dq_dimensions`: object
    - `accuracy`: number
    - `completeness`: number
    - `timeliness`: number
    - `consistency`: number
    - `uniqueness`: number
  - `dq_failed_rules`: string[]
- 规则示例：
  - `date_in_week_range`
  - `daily_sum_matches_total`
  - `amount_non_negative`
  - `duplicate_invoice_id_rate_lt_threshold`

## 三十六、前端性能深化建议（Lit 体系）

- 历史记录列表：
  - 若超过阈值（如 200 条）优先引入 `@lit-labs/virtualizer`。
- 列表渲染：
  - 对稳定 key 的记录使用 `repeat()`；
  - 对高开销派生计算使用 `guard()` 防止重复求值。
- 明细表：
  - 默认只渲染前 N 行并提供“展开全部”，降低首屏渲染负担。

## 三十七、外部资料增量（Iteration 6）

- DQ Scorecard / 维度参考：
  - https://www.ibm.com/think/topics/data-quality-dimensions
  - https://qualytics.ai/data-governance-and-quality/data-quality-scorecard
- Lit 列表与虚拟化：
  - https://lit.dev/docs/templates/lists/
  - https://github.com/lit/lit/blob/main/packages/labs/virtualizer/README.md

## 三十八、下一轮行动（Iteration 7 计划）

- 将 DQ 契约映射到 `ReportRecord` API 响应类型草案。
- 将 SSE 契约映射到 `control-ui/src/ui/types.ts` 的事件类型定义草案。
- 形成“最小实现 PR 切分建议”（后端事件流 PR / 前端接入 PR / 可访问性 PR）。

---

## 三十九、本轮新增结论（Iteration 7）

- 已完成“最小实现 PR 切分建议”，可以直接进入开发排期。
- 已补充周报页信息分层（progressive disclosure）策略，减少首屏认知负担。
- 已补充 SSE 多实例部署要点（粘性会话 + `Last-Event-ID` 恢复策略）。
- 门禁验证通过：
  - code-review agent：PASS（无阻塞问题）
  - test-agent：PASS（`7 passed, 0 failed`）

## 四十、最小实现 PR 切分建议（3 PR）

## PR-1：后端事件流基础层（低风险、先落地）

- 目标：
  - 建立事件存储与查询能力，不改前端行为。
- 涉及文件：
  - `backend/reports/models.py`
  - `backend/reports/llm_analyzer.py`
  - `backend/server/api/routes_reports.py`
- 交付内容：
  - `report_analysis_events` 表 + 索引；
  - `emit_analysis_event()` 写入 pending/running/done/failed；
  - 增量查询接口 `/events?after_id=...`。
- 验收：
  - 单测覆盖事件写入与查询顺序；
  - 现有报告流程无回归（7+ 用例通过）。

## PR-2：前端 SSE 接入层（中风险、可回退）

- 目标：
  - 接入 EventSource，失败自动回退现有轮询。
- 涉及文件：
  - `control-ui/src/ui/controllers/reports.ts`
  - `control-ui/src/ui/types.ts`
- 交付内容：
  - `start/stopAnalysisEventStream`；
  - SSE 错误计数与退避回退；
  - done/failed 后强制详情对齐。
- 验收：
  - 模拟断流后仍能到达 done/failed；
  - 不影响未开启 SSE 的旧流程。

## PR-3：可访问性 + 信息分层（中风险、体验增益）

- 目标：
  - 提升周报页可读性与可达性。
- 涉及文件：
  - `control-ui/src/ui/views/reports-tab.ts`
  - `control-ui/src/i18n/locales/zh-CN.ts`（必要时）
  - `control-ui/src/i18n/locales/en.ts`（必要时）
- 交付内容：
  - `aria-live` 状态播报；
  - 历史记录与 tab 的 aria 属性；
  - 首屏仅展示 3-5 核心 KPI，明细折叠后按需展开（progressive disclosure）。
- 验收：
  - 键盘可达测试通过；
  - 对比度与状态播报检查通过。

## 四十一、交付节奏建议（1 周）

- Day 1-2：PR-1（后端事件流基础）
- Day 3-4：PR-2（前端 SSE + 回退）
- Day 5：PR-3（可访问性与信息分层）
- 并行条件：
  - PR-3 可与 PR-2 并行；
  - PR-2 对 PR-1 有接口依赖（至少需 `/events` 查询接口）。

## 四十二、Iteration 7 验收模板（建议直接复用）

- 功能门禁：
  - [ ] 报告生成成功后有完整事件序列（pending->running->done/failed）
  - [ ] SSE 断开后自动切换轮询并最终收敛状态
- 质量门禁：
  - [ ] `reanalyze` 脏数据场景返回 422（非 500）
  - [ ] 并发重跑冲突返回 409
- 体验门禁：
  - [ ] 首屏 KPI 数量 <= 5 且 5 秒可扫描
  - [ ] 键盘可完成“选记录-看详情-重试分析”主路径

## 四十三、外部资料增量（Iteration 7）

- SSE 多实例恢复：
  - https://stackoverflow.com/questions/58335110/some-of-server-sent-events-lost-while-eventsource-reconnecting
  - http://www.singhajit.com/server-sent-events-explained/
- 仪表盘信息分层（Progressive Disclosure）：
  - https://quality.arc42.org/approaches/progressive-disclosure
  - https://www.boundev.com/blog/dashboard-design-best-practices-guide

## 四十四、下一轮行动（Iteration 8 计划）

- 输出 PR-1 的“字段级变更清单 + 单测清单”（可直接交给开发）。
- 输出 PR-2 的“controller 状态机图 + 错误恢复流程图”。
- 输出 PR-3 的“无障碍验收脚本（手测步骤）”。
